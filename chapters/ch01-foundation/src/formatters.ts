export function formatHeading(value: string): string {
  return value.trim();
}

export function formatCaption(value: string): string {
  return value.trim();
}

export function formatMetricValue(value: string): string {
  return value.toUpperCase();
}